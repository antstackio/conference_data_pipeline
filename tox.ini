[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
    pyspark
commands = pytest --cov --cov-report=term --cov --cov-report=xml --cov-config=tox.ini --cov-branch
 
[coverage:run]
omit = 
  /local_disk0/*
  /databricks/*